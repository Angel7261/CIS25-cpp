#include <iostream>

int main() {
    int countdown = 100;
    while (condition > 0) {
int number;
std::cout << "Enter a number between 1 and 10: ";
std::cin >> number;

// While the input is invalid...
while (number < 1 || number > 10) {
    // ...scold the user and ask again.
    std::cout << "Invalid input. Please enter a number between 1 and 10: ";
    std::cin >> number;
}

// If the code reaches here, the number is guaranteed to be valid.
std::cout << "You entered " << number << std::endl;

int x = 1; // initialization

 while x <= 5) { // Test
    std:cout << "Hello ! X is now"  << x << std::endl;
    
return 0;
}